#include <WiFi.h>
#include <HTTPClient.h>

const char* ssid = "YourWiFiSSID";
const char* password = "YourWiFiPassword";
const char* server = "yourwebsite.com"; // Change this to your website URL
const String path = "/receive_data.php"; // Change this to the path of your PHP script

void setup() {
  Serial.begin(115200);
  delay(1000);
  
  WiFi.begin(ssid, password);
  while (WiFi.status() != WL_CONNECTED) {
    delay(1000);
    Serial.println("Connecting to WiFi...");
  }
  Serial.println("Connected to WiFi");

  pinMode(BUZZER, OUTPUT);
}

void loop() {
  // Your existing code to measure weight and calculate liter and val
  
  // Send data to your server
  sendData(weight, liter, val);
  
  delay(5000); // Adjust the delay as needed
}

void sendData(float weight, int liter, int val) {
  WiFiClient client;
  
  if (client.connect(server, 80)) {
    String data = "weight=" + String(weight) + "&liter=" + String(liter) + "&val=" + String(val);
    
    client.println("POST " + path + " HTTP/1.1");
    client.println("Host: " + String(server));
    client.println("Content-Type: application/x-www-form-urlencoded");
    client.println("Content-Length: " + String(data.length()));
    client.println();
    client.println(data);
    
    client.stop();
  }
}
